services:
  - type: web
    name: raitha-bandava
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    runtime: python
    pythonVersion: "3.11.11"