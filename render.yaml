services:
  - type: web
    name: raitha-bandava
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 300 --workers 1 main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
